# Admin Dashboard API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# Note: JWT tokens are automatically managed by the application
# They are stored in secure HTTP-only cookies after login
# No manual token configuration needed here
